#### формула - [[слово]] в [[алфавит логики высказываний| алфавите логике высказываний ]]
- $\top, \bot, \forall i \in w: v_i$ - атомные(элементарные) формулы
- если $a, b$ формулы, тогда следующие слова тоже являются формулами:
	- $a \& b$
	- $a|b$
	- $a \rightarrow b$
	- $\neg a$ 

- формула называется 
	- тавтологией, если $\forall \gamma: \gamma(\phi) = 1$ 
	- выполнимой, если $\exists \gamma: \gamma(\phi) = 1$
	- невыполнимой, если $\forall \gamma: \gamma(\phi) = 0$ 
***
#дискретная-математика #логика-высказываний #понятие 