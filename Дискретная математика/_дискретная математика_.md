## билеты
```dataview
LIST
FROM "Дискретная математика/билеты"
SORT number(replace(replace(file.name, "билет ", ""), ".md", ""))
```
## условные обозначения
- $\in$ - принадлежит
- $:=$ - равно по определению
- $\forall$ - для всех
- $\exists$ - существует хотя бы одно
- $\subseteq$ - является подмножеством 
- <=>/<->- тогда и только тогда/равносильно
- =>/->- отсюда следует
- & - логическое и
- v - логическое или
- $\neg$ - логическое отрицание
- $\varnothing$ - пустое множество
- $\cap$ - пересечение
- $\cup$ - объединение
- $\triangle$ - симметричная разность 
- $\bar{x}$ - кортеж/набор элементов 
- $f: A \to B$ - отображение 
- $B^A$ - множество всех отображений
- $\overset{1:1}{\longrightarrow}$ - [[инъективное отображение]]
- $\twoheadrightarrow$ - [[сюръективное отображение]]
- $\rightarrowtail$ - [[всюду определённое отображение]]
- $\overset{1-1}{\twoheadrightarrow}$ - [[биектиное отображение]]
- $(f \circ g)(a)$ - [[композиция функций]]
- $[a]_{\sim}$ - [[класс эквивалентности]] 
- $A_{prop}$ - [[алфавит логики высказываний]]
- $(\varphi \leftrightarrow \psi)$ - $((\varphi \to \psi) \land (\psi \to \varphi))$ 
## множество
- **определения**
```dataview
LIST
FROM "Дискретная математика/Множество"
WHERE contains(file.tags, "#понятие")
SORT file.name ASC
```
- **теоремы и леммы**
```dataview
LIST
FROM "Дискретная математика/Множество"
WHERE contains(file.tags, "#множество") AND (contains(file.tags, "#теорема") OR contains(file.tags, "#лемма"))
SORT file.name ASC
```
- **билеты связанные с этой темой**
```dataview
LIST
FROM "Дискретная математика/билеты"
WHERE contains(file.tags, "#множество")
SORT number(replace(replace(file.name, "билет ", ""), ".md", ""))
```
## отношения
- **определения** 
```dataview
LIST
FROM "Дискретная математика/отношения"
WHERE contains(file.tags, "#понятие")
SORT file.name ASC
```
- **алгоритмы**
```dataview
LIST
FROM "Дискретная математика/отношения"
WHERE contains(file.tags, "#алгоритм")
SORT file.name ASC
```
- **билеты**
```dataview
LIST
FROM "Дискретная математика/билеты"
WHERE contains(file.tags, "#отношения")
SORT number(replace(replace(file.name, "билет ", ""), ".md", ""))
```
## отображение и функции
- **определения**
```dataview
LIST
FROM "Дискретная математика/отображения и функции"
WHERE contains(file.tags, "#понятие")
SORT file.name ASC
```
## логика высказываний
- **определения**
```dataview
LIST
FROM "Дискретная математика/логика высказываний"
WHERE contains(file.tags, "#понятие")
SORT file.name ASC
```
- **теоремы и леммы**
```dataview
LIST
FROM "Дискретная математика/логика высказываний"
WHERE contains(file.tags, "#лемма") OR contains(file.tags, "#теорема")
SORT file.name ASC
```
## исчисление высказываний
- **определения**
```dataview
LIST
FROM "Дискретная математика/исчисление высказываний"
WHERE contains(file.tags, "#понятие")
SORT file.name ASC
```
- **аксиомы и правила**
```dataview
LIST
FROM "Дискретная математика/исчисление высказываний"
WHERE contains(file.tags, "#аксиома-правило")
SORT file.name ASC
```

## логика предикатов
- **определения**
```dataview
LIST
FROM "Дискретная математика/логика предикатов"
WHERE contains(file.tags, "#понятие")
SORT file.name ASC
```
- **теоремы и леммы**
```dataview
LIST
FROM "Дискретная математика/логика предикатов"
WHERE contains(file.tags, "#лемма") OR contains(file.tags, "#теорема")
SORT file.name ASC
```
