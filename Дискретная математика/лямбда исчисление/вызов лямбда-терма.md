#### вызов по значению - в подтермах вида $((\lambda x.t)s)$ мы сначала переписываем  $s$ приводя его к неупрощаемому виду $s^\prime$ и только потом применяем [[b-редукция|b-редукцию]] и записываем $t[x = s^\prime]$ 
#### вызов по имени - в подтермах вида $((\lambda x.t)s)$ мы сначала применяем [[b-редукция|b-редукцию]] и далее упрощаем терм $t[x = s]$ 
***
#понятие #дискретная-математика #лямбда-исчисление 