#### минимизация - функция $\mu y(f(\bar x, y) = 0)$, которая ищет наименьший $y$, чтобы функция $g(\bar x, y)$ принимала значение $0$   

##### свойства
- если такого $y$ нет, то функция не определена 
***
#понятие #дискретная-математика #частично-рекурсивные-функции 