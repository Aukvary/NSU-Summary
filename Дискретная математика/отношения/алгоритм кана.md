#### алгоритм кана - построение для [[бинарные отношения|отношения]] $r$ [[топологическая сортировка|топологической сортировки]] $r^L$ 

```
S = A
L #пустная последовательность упорядоченных элментов из A

while S is non-empty do
	remove a node n from S
	add n to tail of L
	for each node m with an edge e from n to m do
		remove edge e from graph
		if m has no other incomming edges then
			insertm into S
			
if graph has edges then
	return error
```
***
#алгоритм #дискретная-математика #отношения 