- ядро находит исполняемый файл и проверяет права доступа
- адресное пространство текущего процесса полностью заменяется сегментами кода, данных и кучи из нового файла
- инициализируется стек главного потока и регистры процессора
- управление передается на точку входа (функцию main) новой программы
```C
// пример использования совместно с fork
if (fork() == 0) {
    execl("/bin/ls", "ls", "-l", NULL), // текущий процесс превращается в ls
}
```
***
#алгоритм #конкурентность #императивное-программирование