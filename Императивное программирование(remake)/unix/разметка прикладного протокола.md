#### разметка прикладного протокола - способ структурирования данных в [[сериализация|сериализованном]] сообщении с использованием разделителей полей и терминатора сообщения, что позволяет десериализатору однозначно восстанавливать объекты

##### использование специальных символов (например, # для полей и $ для конца сообщения) позволяет обрабатывать сообщения переменной длины без предварительного знания их размера
#### сигнатура
```C
#include <stdint.h>

// структура запроса в памяти
struct calc_proto_req_t {
    int32_t id,
    int method, // тип операции (например, add)
    double operand1,
    double operand2,
},
// пример сериализованной строки: "1000#ADD#1.5#5.6$"
```
***
#понятие #ipc #императивное-программирование