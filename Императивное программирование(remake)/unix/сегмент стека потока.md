#### сегмент стека потока - изолированная область памяти, выделяемая каждому [[поток|потоку]] при создании для хранения локальных переменных и контекста вызова функций

##### в многопоточных программах на языке с необходимо избегать сохранения адресов локальных переменных в глобальных указателях, так как после завершения потока его стек уничтожается, что превращает такие ссылки в «висячие указатели» и ведет к сбоям
***
#понятие #конкурентность