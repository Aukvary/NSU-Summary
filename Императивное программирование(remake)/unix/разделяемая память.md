#### разделяемая память - механизм взаимодействия процессов (ipc), предоставляющий общую область оперативной памяти для хранения переменных и массивов

##### в отличие от работы с файлами, этот метод использует настоящую оперативную память и требует явной инициализации и привязки сегментов в каждом процессе
***
#понятие #ipc #императивное-программирование 