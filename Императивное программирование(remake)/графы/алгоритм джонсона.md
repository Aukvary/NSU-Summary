- в граф добавляется новая вершина $s$, из которой проводятся рёбра с нулевым веком во все остальные вершины
- запускаем [[алгоритм беллмана-форда]] из вершины $s$ 
- для каждого ребра $(u, v)$ c исходным весом $w(u, v)$ высиляется новый вес $w^\prime (u, v)$ по формуле $w^\prime(u, v) = w(u, v) + h(w) - h(v)$, это делает все веса неотрицательными
- запускаем [[алгоритм дейкстры]] из каждой вершины 
- после чего мы возвращаем веса $d(u, v) = d^\prime(u, v) + h(v) - h(u)$ 
```C

```
***
#алгоритм #императивное-программирование 