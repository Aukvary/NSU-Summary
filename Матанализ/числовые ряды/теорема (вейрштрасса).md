#### если функция $f(x)$ непрерывна на отрезке $[-\pi, \pi]$ и имеет период $2\pi$, то существует последовательность тригонометрических многочленов, которая сходится к $f(x)$ на этом отрезке 

##### доказательство
- 
***
#теорема #матанализ 